// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Wei-Shaw/sub2api/ent/redeembatch"
)

// RedeemBatch is the model entity for the RedeemBatch schema.
type RedeemBatch struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Config holds the value of the "config" field.
	Config map[string]interface{} `json:"config,omitempty"`
	// CodeCount holds the value of the "code_count" field.
	CodeCount int `json:"code_count,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy int64 `json:"created_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RedeemBatchQuery when eager-loading is set.
	Edges        RedeemBatchEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RedeemBatchEdges holds the relations/edges for other nodes in the graph.
type RedeemBatchEdges struct {
	// RedeemCodes holds the value of the redeem_codes edge.
	RedeemCodes []*RedeemCode `json:"redeem_codes,omitempty"`
	// WelfareBalances holds the value of the welfare_balances edge.
	WelfareBalances []*WelfareBalance `json:"welfare_balances,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RedeemCodesOrErr returns the RedeemCodes value or an error if the edge
// was not loaded in eager-loading.
func (e RedeemBatchEdges) RedeemCodesOrErr() ([]*RedeemCode, error) {
	if e.loadedTypes[0] {
		return e.RedeemCodes, nil
	}
	return nil, &NotLoadedError{edge: "redeem_codes"}
}

// WelfareBalancesOrErr returns the WelfareBalances value or an error if the edge
// was not loaded in eager-loading.
func (e RedeemBatchEdges) WelfareBalancesOrErr() ([]*WelfareBalance, error) {
	if e.loadedTypes[1] {
		return e.WelfareBalances, nil
	}
	return nil, &NotLoadedError{edge: "welfare_balances"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RedeemBatch) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case redeembatch.FieldConfig:
			values[i] = new([]byte)
		case redeembatch.FieldID, redeembatch.FieldCodeCount, redeembatch.FieldCreatedBy:
			values[i] = new(sql.NullInt64)
		case redeembatch.FieldName:
			values[i] = new(sql.NullString)
		case redeembatch.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RedeemBatch fields.
func (_m *RedeemBatch) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case redeembatch.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case redeembatch.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case redeembatch.FieldConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Config); err != nil {
					return fmt.Errorf("unmarshal field config: %w", err)
				}
			}
		case redeembatch.FieldCodeCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field code_count", values[i])
			} else if value.Valid {
				_m.CodeCount = int(value.Int64)
			}
		case redeembatch.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				_m.CreatedBy = value.Int64
			}
		case redeembatch.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RedeemBatch.
// This includes values selected through modifiers, order, etc.
func (_m *RedeemBatch) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRedeemCodes queries the "redeem_codes" edge of the RedeemBatch entity.
func (_m *RedeemBatch) QueryRedeemCodes() *RedeemCodeQuery {
	return NewRedeemBatchClient(_m.config).QueryRedeemCodes(_m)
}

// QueryWelfareBalances queries the "welfare_balances" edge of the RedeemBatch entity.
func (_m *RedeemBatch) QueryWelfareBalances() *WelfareBalanceQuery {
	return NewRedeemBatchClient(_m.config).QueryWelfareBalances(_m)
}

// Update returns a builder for updating this RedeemBatch.
// Note that you need to call RedeemBatch.Unwrap() before calling this method if this RedeemBatch
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RedeemBatch) Update() *RedeemBatchUpdateOne {
	return NewRedeemBatchClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RedeemBatch entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RedeemBatch) Unwrap() *RedeemBatch {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RedeemBatch is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RedeemBatch) String() string {
	var builder strings.Builder
	builder.WriteString("RedeemBatch(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("config=")
	builder.WriteString(fmt.Sprintf("%v", _m.Config))
	builder.WriteString(", ")
	builder.WriteString("code_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.CodeCount))
	builder.WriteString(", ")
	builder.WriteString("created_by=")
	builder.WriteString(fmt.Sprintf("%v", _m.CreatedBy))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RedeemBatches is a parsable slice of RedeemBatch.
type RedeemBatches []*RedeemBatch
