# 闲鱼自动发货：未完成任务清单

> 审查日期：2026-08-30  
> 本文仅保留尚未完成、当前环境无法复现或仍需外部动作的事项。已经由代码与本地验证证明完成的页面、接口和修复项不再保留。

## 边界与完成原则

- 以 `deploy-config/xianyu-auto-reply-src/` 的现有 Worker 实现为唯一 Worker 事实源，只补缺口，不另建账号、登录、商品或发货接口体系。
- 范围仅限自动发货必要管理：Worker 连接、账号、商品映射规则、库存池、发货记录和必要设置。
- Mock、页面可见、接口存在或测试中的固定成功均不能代替真实外部链路验收。
- 下列事项全部关闭前，不得宣称闲鱼自动发货整体完成或可发布。

## P0：Worker 测试证据补齐

- [x] 在隔离且依赖受控的环境按 `deploy-config/xianyu-auto-reply-src/backend-web/requirements-test.txt` 安装测试依赖，执行 `backend-web/tests/test_internal_api.py` 并保存结果。
  - 已补齐 requirements-test.txt 缺失的 import 链依赖（asyncmy/requests 等），干净 venv 仅按该文件安装后 19 项测试全部通过（2026-08-30 复现）。
  - 证据：`deploy-config/test-evidence-worker-contract-2026-08-30.txt`（完整输出）。
  - 验收：测试可正常收集且全部通过；不得修改生产语义来迁就测试环境。

## P0：构建、审查并切换 Worker 镜像

- [ ] 从当前仓库的 vendored Worker 源码构建新镜像，确认镜像实际包含 launcher、`/api/v1/internal/*`、服务身份认证和 delivery-results 回传实现。
- [ ] 对当前 Worker 源码与构建产物完成独立高风险复审，核对认证/所有者边界、扫码登录状态流、消息发送三态回执、delivery-results 回传、并发/幂等和失败关闭行为。
- [ ] 取得新镜像 digest，并替换部署环境中的 `XIANYU_WORKER_IMAGE_DIGEST`；不得继续使用文档明确标记的旧摘要 `sha256:8343c385...46d5`。
- [ ] 按 `deploy-config/compose.yml` 和 `deploy-config/sub2api-ops.md` 部署新镜像，验证三个 Worker 进程、健康检查、内网可达性和双向 token 配置。
  - 验收：运行中容器 digest 与本次已审源码构建产物一致，旧镜像不再承担当前职责，部署证据可复核。

## P0：真实外部链路验收

在可控测试账号和测试商品上逐项执行；每项需保留请求、状态流转和最终平台结果证据，不能用 Mock 替代。

- [ ] 服务间认证：有效 token 可访问内部路由；缺失/错误 token 失败关闭；服务身份仍绑定现有管理员/所有者语义。
- [ ] 首个账号登录：空账号状态生成 `session_id` → 扫码轮询 → Worker 保存 Cookie → WebSocket 任务启动 → 主程序账号投影可见。
- [ ] 账号生命周期：停用会停止真实任务，启用会恢复真实任务；重新登录/刷新 Cookie 与退出/清除凭证均产生对应 Worker 状态变化并正确投影。
- [ ] 商品与规则：Worker 商品同步到主程序；手工绑定/解绑有效；关键词规则优先、账号默认池兜底，且不覆盖手工映射、不允许绑定停用池。
- [ ] 库存与发货：订单事件只 claim 一份库存 → 复用现有 send-message → 收到平台明确回执 → delivery-results 更新同一发货记录；超时/未知不得写成成功。
- [ ] 原码补发：使用原 claim 和原 `chat_id` 走同一发送路径，收到明确回执后更新原记录，不领取新库存、不重复扣减。
- [ ] 异常可观测性：断开 Worker、令 token 失效并制造一次同步/发送失败，确认概览、列表和设置页展示真实错误，不回退为“未知”或伪造的零数据。

## P0：独立复审闭环

- [x] 恢复可用且已认证的 Claude 跨模型只读审查入口（Codex，经 corealgos 网关；修复 app-server 认证抖动后可用），对当前最终差异完成多轮复审。
- [x] 消费复审结论并做了**基座底层重构**（用户裁定：”从基座底层逻辑设计，不叠补丁”）：
  - 26 轮 adversarial review 收敛了 dispatched 三态 / Future 清理 / attempt 贯穿 / 双关闭路径 / 后台任务生命周期 / 超时窗口 / 终态语义等边界；
  - 识别出基座根因并重构：attempt_count 基线统一（迁移 234）、中心化状态转换原语（`xianyu_order_claim_state.go`）、统一回执枚举（`ReceiptOutcome`）、回执 Future 收口（`ReceiptRegistry`）、worker client 信封解码与错误细分；
  - 旧链彻底清理：`FailResendClaim`/`MarkResendSent`/`XianyuWorkerResult` 旧结构/`systemUserID` 死参数/旧三套回执编码已删除，残留扫描通过。
  - 验证：Go 完整测试 50 包 ok、Worker 契约测试 19 项通过、镜像重建+三进程冒烟通过；集成测试（含 bug A：Claim attempt=0→自动发货回执→sent）待 DB 环境确认。

## 最终关闭检查

- [ ] 确认上述测试、镜像、部署、真实链路和独立复审均有可独立复核的证据。
- [ ] 再次扫描旧 Worker 路径、旧镜像摘要、重复响应解包、平行账号/登录/商品模型及过时部署入口；发现残留则删除，或记录明确的负责人、截止时间和清理触发条件。
- [ ] 确认工作树中的新增文件均被版本控制纳入，最终差异只保留当前单一路径实现和与之同步的测试、文档。
